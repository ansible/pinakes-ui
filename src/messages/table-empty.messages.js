const { defineMessages } = require('react-intl');

const tableEmptyMessages = defineMessages({
  noRecords: {
    id: 'tableEmpty.noRecords',
    defaultMessage: 'No records'
  }
});

export default tableEmptyMessages;
