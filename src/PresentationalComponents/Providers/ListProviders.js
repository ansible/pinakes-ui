import React from 'react';
import { Section } from '@red-hat-insights/insights-frontend-components';

const ListProviders = () => {
    return (
        <Section type='content'>
            <h1> Rule List </h1>
        </Section>
    );
};

ListProviders.displayName = 'list-providers';

export default ListProviders;
